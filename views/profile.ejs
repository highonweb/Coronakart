<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Coronacopia</title>
    <style>
      .profile {
        width: 100%;
        min-height: 35vh;
        margin: 30px 0;
        background-image: url('images/probg.jpg');
        padding: 0;
        display: flex;
        flex-flow: row;
        justify-content: space-around;
      }
      .text-wrapper {
        width: 30%;
        text-align: center;
        padding: 10px;
        height: 100%;
      }
      img {
        width: 300px;
        border-radius: 10px;
        border: solid 4px white;
        margin: 30px;
      }

      .info {
        background-color: rgba(26, 26, 26, 0.6);
        color: white;
        display: flex;
        align-items: center;
        justify-content: center;
        border-radius: 8px;
        padding: 25px 30px;
        height: auto;
        margin: 40px;
        flex-flow: column;
      }
      body {
        margin: 0;
      }
      .text {
        background-color: rgba(26, 26, 26, 0.6);
        border-bottom: 1px solid rgba(10, 10, 10, 0.8);
        display: block;
        margin: 0;
        font-size: 22px;
        width: 100%;
        padding: 15px;
        transition: 500ms;
      }
      .text:hover {
        border-left: 5px solid rgb(253, 129, 149);
      }
    </style>
  </head>
  <body>
    <div class="profile">
      <div class="text-wrapper info">
        <div class="text"><%= user.name %></div>
        <div class="text"><%= user.usertype %></div>
        <div class="text"><%= user.email %></div>
      </div>
      <div class="text-wrapper">
        <img
          src="https://api.adorable.io/avatars/3
        00/<%= user.name %> "
        />
      </div>
    </div>
    <% for( let i = 0; i < user.products.length; i++ ) { %>
    <div class=""><%= user.products[i] %></div>
    <% } %> <% for( let i = 0; i < user.history.length; i++ ) { %>
    <div class=""><%= user.history[i] %></div>
    <% } %>
  </body>
</html>
