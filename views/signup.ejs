<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8" />
    <meta
      name="viewport"
      content="width=device-width, initial-scale=1.0, shrink-to-fit=no"
    />
    <title>Strong Ties</title>
    <meta
      name="description"
      content="An app that will be in your bookmarks once you click this link
so click cautiously"
    />
    <link rel="stylesheet" href="signup.css" />
  </head>

  <body>
    <div class="box">
      <form method="post" action="/<%= action %> ">
        <h2 class="text-center">
          <strong>Create</strong> an <% if (action =='seller') { %> seller <% }
          %> account.
        </h2>
        <input type="text" name="name" placeholder="Username" autofocus="" />
        <input type="email" name="email" placeholder="Email" autofocus="" />

        <input type="password" name="password" placeholder="Password" />
        <input
          type="password"
          name="passwordconf"
          placeholder="Confirm Password"
        />

        <button class="signup" type="submit">Sign Up</button>
        <a
          class="already"
          href="<% if (action == 'seller') { %>
         signinseller
        
        <% } else { %>
          signin
          <% } %>"
          >You already have an account? Login here.</a
        >
      </form>
    </div>

  </body>
</html>
